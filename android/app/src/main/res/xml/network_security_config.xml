<?xml version="1.0" encoding="utf-8"?>
<!--
  Budget Beacon ships local-first; production traffic is HTTPS only.
  Cleartext is allowed for localhost (Capacitor live-reload + local AI providers
  like Ollama on http://localhost:11434) so devs can run the app against a
  local LAN model without disabling system-wide cleartext.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
