# Changelog

Use this file for meaningful repo-visible change history. Keep transient task chatter in `TODO.md` or `WHERE_LEFT_OFF.md`.

## Unreleased

- **M10.4 wiring + sync hardening (2026-05-04).** `syncService` closure bug fixed — each Yjs↔Dexie observer now captures its own `table` via a per-table `wireTable` helper (was leaking the loop variable, so all observers wrote to the last table). `bootstrap(householdId, key, wsUrl?)` now treats an empty/null `wsUrl` as **local-only mirroring**: no `WebsocketProvider` is constructed, so the app no longer attempts to dial the placeholder `wss://sync.budgetbeacon.app`. Added `getStatus()` / `onStatusChange()` and a `disconnect()` method. New `SyncStatusBadge` component (sidebar footer + mobile header). New `AuthSyncCard` mounts in Settings — opt-in signup/login + relay URL input + "Start sync" button; logging out clears the in-memory household key without touching local Dexie data. **174 tests** still pass; `audit:controls` baseline unchanged (`setTimeout=5 alert=0`).
- **Debt trajectory sparkline + pure downsample (2026-05-03).** **`downsamplePayoffTrajectory`** moved to **`src/modules/debt-strategy/trajectoryDownsample.ts`** (used by **`StrategyComparison`** payoff sparklines). Last-point append uses **`month` / `totalBalance`** equality so the stride does not duplicate the terminal sample. Vitest **`trajectoryDownsample.test.ts`**. Suite **`164`** tests; **`npm run validate`** and **`audit:controls`** green.
- **Debt Center what-if UX + M9 QA doc (2026-05-03).** **`StrategyComparison`**: preset chips ($0 / $50 / … / $1k), **range slider** (0–$3k) alongside numeric extra payment, helper copy when slider caps below typed value, **`aria-*`** on presets/slider. New **`docs/M9_ANDROID_QA_CHECKLIST.md`** (device install, safe-area, import/OCR, assistant, PWA); **`UNGATED_PRODUCT_BACKLOG`** Tier A links it. **`161`** tests; **`npm run validate`** green.
- **Assistant grounding + CI pipeline (2026-05-03).** `collectAssistantPromptFacts` pulls **counts** for **`taxRecords`**, **`taxForms`**, **`documents`**, **`payeeRules`** into the system prompt and **`AssistantContextFacts`** (no blobs/PII). **`beaconPlaceholderReply`** summarizes those counts when non-zero. Docs: **`docs/ASSISTANT_CONTEXT_COVERAGE.md`** maintainer checklist; **`UNGATED_PRODUCT_BACKLOG`** Tier A cross-link. GitHub Actions **`.github/workflows/ci.yml`** runs **`npm run validate`** on push/PR to `main`/`master`. New tests (**`161`** total).
- **Expansion backlog + demo reset API (2026-05-03).** **`resetToBundledDemo()`** in **`seedDemoData.ts`** centralizes wipe+seed (Settings uses it). **`docs/UNGATED_PRODUCT_BACKLOG.md`** prioritizes Tier A–D work while **M10/M11** stay gated. **`ROADMAP.md`** M9 blurb matches current backup help text. Beacon Bridge header badge: accessible label + **`aria-hidden`** icon. **`resetToBundledDemo.test.ts`** (Vitest + **`fetch`** mock). **`PLAN.md`** / **`TODO.md`** link the backlog. **`159`** tests — **`npm run validate`** green.
- **Destructive UX — shared copy + close-before-run (2026-05-03).** New **`fullDatabaseWipeCopy.ts`** (title, scope paragraph, backup hint, demo-reset text) reused by cockpit and Settings wipe modals so IndexedDB **`fullDatabaseRwScope`** messaging stays coherent. Confirm actions clear modal state immediately, then **`clearDatabase()`**, to avoid accidental double submits. **`seedDemoData`** JSDoc documents the clear-first requirement. Vitest **`fullDatabaseWipeCopy.test.ts`**; **`158`** tests; **`audit:controls`** baseline unchanged (**`setTimeout=5`**).
- **Destructive actions — Dashboard + Settings modals (2026-05-03).** Cockpit **Wipe All** and Settings **Wipe Local Database** / **Reset to Demo State** use **`BeaconModal`** (Cancel + destructive confirm, Escape/backdrop). **Reset to demo** now **`clearDatabase()`** then **`seedDemoData()`** so seeded data actually replaces existing households (**`seedDemoData`** formerly no-opped when `households.count() > 0`, inconsistent with UI copy).
- **Delete confirmations — Beacon modal pipeline (2026-05-03).** New **`DeleteConfirmProvider`** + **`useDeleteConfirm()`** (`src/context/DeleteConfirmContext.tsx`) renders a branded **`BeaconModal`** instead of **`window.confirm`** for row deletes across Income, Pay Path, Ledger, Debt Center, Stash Map, Credit, Tax Taxi (+ saved forms), Vault, Subscriptions, and Insurance. **`BeaconModal`** backdrop is tagged **`data-beacon-modal-backdrop`** and listens for **Escape**; chat FAB **`Escape`** handler ignores keys while any Beacon overlay is mounted. **`confirmEntityDelete`** retained for non-React call sites with docs pointing at the hook. **`npm run validate`** green (**157** tests).
- **UX — assistant clear + shell a11y (2026-05-03).** Clear chat history uses **`BeaconModal`** instead of `window.confirm`; chat FAB **`aria-expanded` / `aria-controls`**, labeled region **`#beacon-chat-panel`**. Mobile nav menu buttons and **theme toggle** get explicit **`aria-label`** / **`aria-hidden`** icons; suggestion chips **`type="button"`**. Scroll view follows **`streamingText`**. **`npm run validate`** green (**157** tests).
- **Accessibility — icon control labels (2026-05-03).** Descriptive **`aria-label`** on Tax Taxi / Subscriptions / Insurance / Vault header and row actions; Beacon chat Stop/Send; modal close; payee-rule delete. Suite still **157** tests; **`npm run validate`** green.
- **Truthful backup v4 UX + cockpit empty states (2026-05-03).** `exportJson.ts` exports **`BACKUP_FORMAT_VERSION_LABEL`** and **`BACKUP_FORMAT_HELP_TEXT`** (documents, payee rules, replace-on-import, v1–v3 still import). Reports export menu, Settings, and Beacon Bridge consume it — no stale “v3” copy. **Pay Path**: `EmptyState` for zero bills/debts; **Credit Snapshot**: empty chart placeholder, filter-aware `EmptyState` + primary CTA; **aria-label** on Pay Path add/edit. Vitest **`exportJson.constants.test.ts`**; suite **157** tests.
- **Single-source Dexie scope + safer deletes (2026-05-03).** New `src/db/fullDatabaseScope.ts`: `fullDatabaseRwScope()` + `clearFullDatabase()` — `seedDemoData.clearDatabase`, backup **apply**, and tests share one store list (no drift). Shared `confirmEntityDelete()` for icon-button deletes across Income, Pay Path, Debt Center, Stash Map, Credit, Ledger, Tax Taxi, Subscriptions, Insurance, Vault; **aria-label** on those trash buttons. Reports Arena empty tabs use **`EmptyState`**. **`155`** Vitest tests (`fullDatabaseScope.test.ts`).
- **Data integrity — full local wipe (2026-05-03).** `clearDatabase()` clears every Dexie v5 store; Dashboard **Wipe All** matches Settings reset + reload. `npm run validate` and `src/db/clearDatabase.test.ts` (suite now **155** with later scope test).
- **ESLint + hygiene pass (2026-05-03).** `npm run lint` clean: `beaconPlaceholderReply` module; derived onboarding in `App.tsx`; route typing; Tax/Vault/Settings hooks; CSV/OCR regex; prefs initializer; etc.
- **Dashboard + health card polish (2026-05-03).** Global Pressure Index: clamp burn/stash segment widths when Pay Path exceeds income (no negative bar widths); hide misleading 100% surplus when planned income is zero with an explicit copy state; truthful surplus % uses uncapped pressure math. `BudgetHealthScoreCard`: avoid divide-by-zero when `targetSavingsFraction` is **0** on progress bar. Tests remain **153** passing.
- **Mission Control polish (2026-05-03).** Clamp burn/stash progress bars when monthly income is zero; show insurance note when premiums roll into Pay Path; fix goal % when `targetAmount` is zero; align “highest-APR liability” intel with Debt Center’s avalanche simulator (`apr` / legacy `interestRate`); refresh copy to point at payoff timelines in Debt Center (tests still **153**).
- **M7.2 polish (2026-05-03).** Added `src/modules/ai/conversationWindow.ts` to compress older chat turns into a bounded system-summary message while preserving the most recent turns; `BeaconChatbot` now uses this when assembling provider context. Settings `Test Connection` now validates the streaming path by using `provider.chatStream(...)` instead of `chat(...)`. Added `conversationWindow` unit tests; suite now **153** passing.
- **Chatbot fallback test coverage (2026-05-03).** `beaconPlaceholderReply` in `src/modules/ai/beaconPlaceholderReply.ts` (tests via `BeaconChatbot.test.ts`) covers no-provider branches: insurance + MTD category lines, no-expense fallback guidance, and cut/save intent messaging.
- **Reports + Mission Control (2026-05-02).** Month-to-date **expense rollups by category** (`buildExpenseCategoryRollup`, `ExpenseCategoryRollup`) on the Monthly report tab and Mission Control, reusing ledger helpers introduced from the CouplesWealth scavenger pass. **142** unit tests passing.
- **Ledger + exports + AI context (2026-05-02).** Ledger sidebar **top 3 MTD expense categories**; Reports export menu **Expense categories (MTD)** CSV (`expenseCategoriesMtd`); assistant `buildAssistantContext` now includes **insurance in budget summary**, **MTD expense total**, and **top five category lines** aligned with rollups.
- **Dashboard donut + tests + CSV typing (2026-05-02).** **`MtdCategoryDonut`** on cockpit (MTD ledger expense mix); Vitest for **`buildCsvForEntity("expenseCategoriesMtd")`** and **`buildAssistantContext`** (Dexie fixtures; no fake timers in IDB hooks). **`exportCsv`** uses schema types + one narrow helper for optional stored `personId`; **`LedgerRoute`** uses **`LedgerFormValues`** / **`Transaction`** instead of `any`. **`AllocationDonut`** tooltip formatter no longer uses `any`. **144** tests.
- **Assistant facts refactor (2026-05-02).** New **`src/modules/ai/assistantContextFacts.ts`** — **`buildAssistantPromptFacts`** (pure, `now`-injectable) + **`formatAssistantSystemPrompt`**. **`contextBuilder`** delegates to **`collectAssistantPromptFacts`**; **`AssistantContextFacts`** extended for **`monthPrefix`**, MTD rollup fields, **`monthlyInsurance`**, **`budgetStatus`**. **`TEST_STRATEGY.md`** documents Vitest + **`fake-indexeddb`** + **`vi.useFakeTimers`** hazard. **148** tests.
- **No-model chat fallback (2026-05-02).** **`BeaconChatbot`** `placeholderReply` now cites **insurance roll-up**, **MTD ledger spend** with **top three categories** (aligned with Dashboard / Reports / Mission Control), nudges when the month has no expenses, and adds a keyword branch for **category / spend / ledger / expense** questions pointing to those surfaces.
- **M5/M8 carry-overs landed (2026-04-28).**
  - **OFX/QFX parser** at `src/modules/import/parseOfx.ts` — tolerant SGML/XML parser for bank-issued OFX 1.x/2.x and Quicken QFX. Handles `STMTTRN` (bank) and `CCSTMTTRN` (credit-card) blocks; date format YYYYMMDD[HHMMSS]; sign convention `TRNAMT < 0 → expense`. Extracts account metadata (ACCTID, ACCTTYPE, CURDEF). Wired into LedgerImportFlow — bypasses column mapping since OFX is structured.
  - **Payee rules** — new `payeeRules` Dexie table (v5 schema migration). Pure `applyPayeeRules` with `contains` / `exact` match, payee rename, category override, first-match-wins. Settings → Payee Rules panel for CRUD. Auto-applied during CSV and OFX/QFX imports with a "✓ Auto-categorized N of M" banner in the review step. Backup format bumped to **v4** (covers payeeRules); v1/v2/v3 still readable.
  - **Tax forms expanded** — added 1099-MISC, 1099-DIV, 1099-R, 1099-G alongside the existing W-2/1099-NEC/1099-INT/1098/1040.
  - 23 new tests (8 payee rules, 15 OFX parser); **134 total**.
- **M9 partial close (2026-04-28).** Code-split each route via React.lazy + Suspense so the initial bundle stays small. Main bundle dropped from 1.1 MB to 346 KB (gzip 107 KB); each route is a separate 5–35 KB chunk loaded on demand. Recharts and lucide icons split out cleanly. Beacon Bridge route updated to direct users to M10/M11 (sync + joint household) instead of the old M9 framing; copy now references v3 backup format and links to `docs/SYNC_AND_DUAL_ACCOUNT_ARCHITECTURE.md`. Feature flags reorganized — new `syncE2eeCrdt` (M10) and `jointHouseholds` (M11) flags. Real-device APK smoke + safe-area visual QA still open in M9 — require a physical Android.
- **M8 close (2026-04-28).** Real Tax Taxi forms (W-2, 1099-NEC, 1099-INT, 1098, 1040 summary) replacing the 2-field placeholder. Declarative `formDefs.ts` makes adding a form one entry. Multiple forms per type per year now allowed (uses `createId()` instead of fixed slug). New saved-forms list in the Library sidebar with delete + open-to-edit. Honest scope note: captures line-item entries, does not file or compute liability. New debt payoff strategy comparison: pure simulator (avalanche / snowball / minimum) with infeasibility detection; interactive 3-card UI shows months-to-payoff, total interest, and savings vs minimums. Wired into Debt Center. 8 new simulator tests; 111 total.
- **M7.2 close (2026-04-28).** Streaming responses (Ollama JSONL + OpenAI SSE), structured action proposals (Zod-validated `beacon-action` JSON blocks → confirm/dismiss chips → `applyProposedAction` to Dexie), provider health-check button in Settings ("Test Connection"). Bug fix: `AI_CONFIG_ID` was `"default"` while Settings persisted to `"primary"` — every prior session had a silent mismatch; aligned. 16 new tests; 103 total.
- **M7 substantial close (2026-04-28).** Real AI assistant. New `src/modules/ai/` with `AiProvider` interface, `OllamaProvider` (local default), and `OpenAiCompatibleProvider` (cloud opt-in, compatible with OpenAI, Groq, Together, OpenRouter, LM Studio, llama.cpp). `providerFactory` reads the existing `aiConfig` Dexie row. `contextBuilder` injects a real-data system prompt grounded in budget summary + stability index so the model can't fabricate numbers. `BeaconChatbot.tsx` rewritten — status indicator, abort, error surfacing, no-provider fallback shows real aggregates instead of canned replies. `featureFlags.aiAssistantLocal` and `aiAssistantCloud` flipped to true. 16 new tests (URL normalization, response parsing, error wrapping, factory resolution); 87 total. Tool-use / structured action proposals deferred to M7.2 — assistant is read-only this slice.
- **M6 closed (2026-04-28).** Real OCR for the Vault. New `OcrProvider` interface in `src/modules/ocr/`, Tesseract.js browser-side implementation (no network), per-field confidence indicators, edit-before-commit review modal (`VaultExtractionReview`), apply-on-approve flow that writes `incomeSources` / `bills` / `taxRecords` with a `documentId` provenance pointer in `notes`. `featureFlags.ocrLocal` flipped to `true`. 12 new tests; total 71. Audit-controls baseline updated to `setTimeout=5` (5th is the legit `OnboardingWizard` auto-dismiss timer).
- **Roadmap inserted M10 (Auth + Cross-Device Sync) and M11 (Joint Household / Linked Accounts).** Architecture doc at `docs/SYNC_AND_DUAL_ACCOUNT_ARCHITECTURE.md`. Recommended transport: end-to-end-encrypted CRDT (Yjs) over a thin relay — server can never read household data. Old M9 release-candidate work renumbered to M12 to keep release as the last milestone. `INTEGRATIONS_STRATEGY.md` Domain 5 (Beacon Bridge) marked superseded for sync planning.
- **Tooling.** Added `tsx` devDep so `npm run audit:controls` runs on hosts without a global install.

## 1.22.1 (2026-04-12)

- Patch release: closes the downstream-proof fixes uncovered by the first
  `1.22.0` replay and aligns installable semver markers with source tag
  **`v1.22.1`**.
- `bootstrap/update-template.sh` now repairs managed-file mode drift even when
  content already matches the source template, so executable bootstrap surfaces
  recover their `+x` bit during downstream refreshes.
- Integrity manifests now track the real managed installable surface instead of
  transient bytecode or repo-local runtime trees; `distribution/`, `docs/`,
  `notes/`, `.credits-hidden`, `LICENSE`, and `NOTICE` are included.
- Downstream proof on `CandleCompass` exposed and closed the installed-doc
  boundary leak around hard-coded master-template paths; the proof playbook now
  requires sanitizing installed continuity docs when strict validation catches a
  forbidden maintainer source-clone path.

## 1.22.0 (2026-04-12)

- Minor release: preservation-first governance hardening imported from SACST in
  adapted form only, without sysadmin-domain contamination.
- Added `_system/READ_BUNDLES.md`,
  `_system/TEMPLATE_CHANGE_IMPACT_POLICY.md`,
  `_system/SELF_HEALING_BOUNDARY.md`, and
  `_system/VERSION_SENSITIVE_RESEARCH_PROTOCOL.md`.
- Added maintainer-only learning-loop, promotion, harvest-policy, and donor-review
  surfaces in the master-repo meta workspace, plus factory checks
  `_TEMPLATE_FACTORY/check-template-impact.sh` and
  `_TEMPLATE_FACTORY/check-promotion-readiness.sh`.
- Tightened precedence, prompt-emission, host-bundle, load-order,
  operating-profile, awareness, and conflict-detection surfaces so bundle,
  repair, research, and promotion governance stay aligned.
- Regenerated host adapters, `_system/KEY.md`, `_system/SYSTEM_REGISTRY.json`,
  `_system/repo-operating-profile.json`, and `_system/INTEGRITY_MANIFEST.sha256`
  for the 1.22.0 surface.
- `_TEMPLATE_FACTORY/run-maintainer-lane.sh` and
  `_TEMPLATE_FACTORY/run-automation-lane.sh` both passed on 2026-04-12.

## 1.21.1 (2026-04-06)

- Patch release: installable semver and JSON markers (`AIAST_VERSION.md`,
  `_system/.template-version`, `instruction-precedence.json`, `.template-install.json`,
  `aiaast-capabilities.json`) aligned with git tag **`v1.21.1`**; plugin manifests
  `aiast_min_version` **1.21.1**; includes pinned-source upgrade documentation in
  `UPGRADE_AND_DRIFT_POLICY.md` and `GIT_REMOTE_AND_SYNC_PROTOCOL.md` (see
  `AIAST_CHANGELOG.md`).
